tests:
  bluetooth.general:
    extra_args:
      - EXTRA_SETTINGS_OVERLAY_FILES="test.overlay"
    platform_allow:
      - qemu_x86
      - qemu_cortex_m3
      - native_sim
      - native_sim/native/64
    integration_platforms:
      - qemu_x86
    tags: bluetooth
