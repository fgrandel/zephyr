# SPDX-License-Identifier: BSD-3-Clause

description: |
  A property-blocklist, if given, must be a list. This binding should
  cause an error.
schema: block-not-list
include:
  - name: include.yaml
    property-blocklist:
      foo:
